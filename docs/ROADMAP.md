# ROADMAP

`/evolve` skill（`.claude/skills/evolve/SKILL.md`）がこのファイルを読んで、1サイクルにつき1項目だけ拾って実装する。
完了した項目は [docs/roadmap-done.md](./roadmap-done.md) に移す。

## 書式

```
- [ ] <サイズ> <タイトル> — <補足・受け入れ条件>
```

- **サイズ**は `S`（1サイクルで確実に終わる小さな修正）／`M`（1サイクルで終わるが設計判断を伴う）／`L`（複数サイクルにまたがる、または着手前に方針を相談すべき大きな機能）。
- `/evolve` は **`S` と `M` だけを自動で拾う**。`L` は拾わない（人間がサイズを分解して `S`/`M` に落とすか、方針を決めてから着手する）。
- 優先度は上から順。追加するときは狙う位置に挿入する（末尾ではなく）。

## 未着手

- [ ] L ストローク・サーブのキー割り当てをスピン別に変更 — Spaceキー1本ではなく、スピンの向きごとに専用キー（例: cで溜め始め→離すとスライス）で溜め・スイングする方式に変更。既存の溜め/フォアバック判定/サーブ操作全体に関わる設計変更のため、着手前に方針を相談する。
- [ ] S 風向きの表示・変動を調整する — 風向きが細かく変わりすぎないようにし、HUDの風向き表示をもっと目立たせる。
- [ ] S ツーバウンド得点時の表現を改善する — 「エース」等、かっこいい表現にする。
- [ ] M スマッシュのモーションを作る — スマッシュ時にラケットを振り下ろすモーションが見えるようにする。

## 進行中

（なし）
