# ROADMAP

`/evolve` skill（`.claude/skills/evolve/SKILL.md`）がこのファイルを読んで、1サイクルにつき1項目だけ拾って実装する。
完了した項目は [docs/roadmap-done.md](./roadmap-done.md) に移す。

## 書式

```
- [ ] <サイズ> <タイトル> — <補足・受け入れ条件>
```

- **サイズ**は `S`（1サイクルで確実に終わる小さな修正）／`M`（1サイクルで終わるが設計判断を伴う）／`L`（複数サイクルにまたがる、または着手前に方針を相談すべき大きな機能）。
- `/evolve` は **`S` と `M` だけを自動で拾う**。`L` は拾わない（人間がサイズを分解して `S`/`M` に落とすか、方針を決めてから着手する）。
- 優先度は上から順。追加するときは狙う位置に挿入する（末尾ではなく）。

## 未着手

- [ ] M スマッシュのモーションを作る — スマッシュ時にラケットを振り下ろすモーションが見えるようにする。
- [ ] M サーブ・ストロークが入っているように見えるのにフォールト/OUT判定になる問題を調査する — ユーザー提供のスクリーンショット2枚（`docs/image-20260824-001907.png`＝サーブ、`docs/image-20260824-002814.png`＝ストローク、要現物確認）あり。直近の「軌跡とIN/OUT判定のずれ」「サービスフォルト判定と軌跡の不一致」修正では再現しなかったため、別の原因（カメラ視点による見た目の誤差、コート幅の判定式など）がないか調査する。
- [ ] S CPUのバウンド前の先読み（predictBounceApex/`CHASE_APEX_LEAD_MAX`）を再調整する — 直近の「CPUに真正面へ弱いサーブを打ったときの返球が弱い問題」修正で、頂点先読みにクランプ（着地点から`CHASE_APEX_LEAD_MAX`=4.5m）を追加したが、ランダムな着地位置によっては依然stretch=1（ぎりぎり）に戻るケースが残ることを確認済み。実測（手動シミュレーション）しながら`CHASE_APEX_LEAD_MAX`の値を調整する。
- [ ] M CPUの強さがHardでもNormalと比べてそんなに強くない — 各モード（Easy/Normal/Hard）の強さの差を実測して、体感でもはっきり分かるレベルまで明確にする。

## 進行中

（なし）
