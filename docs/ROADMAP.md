# ROADMAP

`/evolve` skill（`.claude/skills/evolve/SKILL.md`）がこのファイルを読んで、1サイクルにつき1項目だけ拾って実装する。
完了した項目は [docs/roadmap-done.md](./roadmap-done.md) に移す。

## 書式

```
- [ ] <サイズ> <タイトル> — <補足・受け入れ条件>
```

- **サイズ**は `S`（1サイクルで確実に終わる小さな修正）／`M`（1サイクルで終わるが設計判断を伴う）／`L`（複数サイクルにまたがる、または着手前に方針を相談すべき大きな機能）。
- `/evolve` は **`S` と `M` だけを自動で拾う**。`L` は拾わない（人間がサイズを分解して `S`/`M` に落とすか、方針を決めてから着手する）。
- 優先度は上から順。追加するときは狙う位置に挿入する（末尾ではなく）。

## 未着手

- [ ] S ドロップショットが時々ネットに掛かる（狙い通り越えるはずの球が落ちる） — `solveShot()` は「ネット上端＋`DROP.CLEARANCE`(0.12m)」を解析的に満たす初速を返すが、実際の飛翔は半陰的オイラー積分（`integrate()`）なので、ネットに達するまで約1秒かかるドロップでは解析解より約3〜6cm低く飛ぶ（誤差 ≒ 0.5·g·t·dt）。`BALL_R`(0.11m) を含む `hitsNet()` の判定と合わせると、ベースラインからのドロップの実測7%（`predictLanding` の1/120刻み。実ステップ1/240でも最悪マージン -0.017m）がネットに掛かる。`tests/smoke.mjs` の「the drop lands right behind the net」がランダムな狙い次第で落ちる（=不安定テスト）のもこれが原因。受け入れ条件：ベースラインから200本のドロップを撃ってネットに掛かるのが0本になること（`DROP.CLEARANCE` を積分誤差ぶん広げる、または `solveShot()` の判定を積分と同じ刻みで行う）。既存のドロップの着地帯（`DROP.Z_MIN`〜`Z_MAX`）は変えないこと。

## 進行中

（なし）
