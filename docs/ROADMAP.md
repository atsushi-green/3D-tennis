# ROADMAP

`/evolve` skill（`.claude/skills/evolve/SKILL.md`）がこのファイルを読んで、1サイクルにつき1項目だけ拾って実装する。
完了した項目は [docs/roadmap-done.md](./roadmap-done.md) に移す。

## 書式

```
- [ ] <サイズ> <タイトル> — <補足・受け入れ条件>
```

- **サイズ**は `S`（1サイクルで確実に終わる小さな修正）／`M`（1サイクルで終わるが設計判断を伴う）／`L`（複数サイクルにまたがる、または着手前に方針を相談すべき大きな機能）。
- `/evolve` は **`S` と `M` だけを自動で拾う**。`L` は拾わない（人間がサイズを分解して `S`/`M` に落とすか、方針を決めてから着手する）。
- 優先度は上から順。追加するときは狙う位置に挿入する（末尾ではなく）。

## 未着手

- [ ] S ネットポストの位置を実測値に合わせる（`config.js` の `COURT.NET_HALF: 5.75` → `6.40`） — ITF規定ではポストはダブルスサイドラインの外側0.914m（=中心から6.40m）。現状は内寄りで、サイド際のネット判定（`netHeightAt`）にもわずかに影響する。`tests/smoke.mjs` が通ることを確認するだけでよい単純な数値修正。

- [ ] L ボールにスピン（トップスピン／スライス／フラット）を実装する — 詳細未設計。重力(`PHYSICS.GRAVITY`)とボール半径(`PHYSICS.BALL_R`)が意図的に非現実値（アーケード寄り）になっているため、マグヌス力を実式で足すと効果が見た目に乗りにくい可能性がある。着手前に「アーケードのまま感覚チューニングするか」「重力・ボールサイズを実測寄りに戻すか」を人間と相談すること（2026-08-17の会話で論点として提示済み）。→人間と相談して、アーケードkのまま感覚チューニングすることで合意。

- [ ] L 風（弱いランダムな横風）を実装する — スピンの実装方針（力の掛け方の設計）が固まってから着手した方が、物理モデルの一貫性を保ちやすい。
- [ ] ボールを打つタイミングでもコースが切り替わるようにしてください。早いタイミングで打てば引っ張るし、振り遅れると流れていくイメージです。
- [ ] 

## 進行中

（なし）
