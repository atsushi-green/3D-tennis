# ROADMAP

`/evolve` skill（`.claude/skills/evolve/SKILL.md`）がこのファイルを読んで、1サイクルにつき1項目だけ拾って実装する。
完了した項目は [docs/roadmap-done.md](./roadmap-done.md) に移す。

## 書式

```
- [ ] <サイズ> <タイトル> — <補足・受け入れ条件>
```

- **サイズ**は `S`（1サイクルで確実に終わる小さな修正）／`M`（1サイクルで終わるが設計判断を伴う）／`L`（複数サイクルにまたがる、または着手前に方針を相談すべき大きな機能）。
- `/evolve` は **`S` と `M` だけを自動で拾う**。`L` は拾わない（人間がサイズを分解して `S`/`M` に落とすか、方針を決めてから着手する）。
- 優先度は上から順。追加するときは狙う位置に挿入する（末尾ではなく）。

## 未着手

- [ ] L ストローク・サーブのキー割り当てをスピン別に変更 — Spaceキー1本ではなく、スピンの向きごとに専用キー（例: cで溜め始め→離すとスライス）で溜め・スイングする方式に変更。既存の溜め/フォアバック判定/サーブ操作全体に関わる設計変更のため、着手前に方針を相談する。
- [ ] M スマッシュのモーションを作る — スマッシュ時にラケットを振り下ろすモーションが見えるようにする。
- [ ] S CPU/AIのフットワーク速度を調整する — 敵の動きが速すぎるので、`PLAYER.CPU_CHASE`/`CPU_RECOVER`等を見直す。
- [ ] S 溜めによる威力差をより顕著にする — TAP_T/CHARGE_T等、弱溜めと強溜めの差をもっと体感しやすくする。
- [ ] M CPUに真正面へ弱いサーブを打ったときの返球が弱い問題を調査する — コースを指定せず威力も弱めでサーブしたときに限って、CPU側の返球が弱いことが多いとの報告（近い場所へのサーブなのでCPUも溜めて強く返せるはず）。直近のCPU返球強化（stretch/chasePosition周り）でカバーしきれていないケースがないか調査する。
- [ ] M サーブが入っているように見えるのにフォールト判定になる問題を調査する — ユーザー提供のスクリーンショット（image-20260824-001907.png、要現物確認）あり。直近の「軌跡とIN/OUT判定のずれ」「サービスフォルト判定と軌跡の不一致」修正では再現しなかったため、別の原因（カメラ視点による見た目の誤差、コート幅の判定式など）がないか調査する。

## 進行中

（なし）
