# ROADMAP

`/evolve` skill（`.claude/skills/evolve/SKILL.md`）がこのファイルを読んで、1サイクルにつき1項目だけ拾って実装する。
完了した項目は [docs/roadmap-done.md](./roadmap-done.md) に移す。

## 書式

```
- [ ] <サイズ> <タイトル> — <補足・受け入れ条件>
```

- **サイズ**は `S`（1サイクルで確実に終わる小さな修正）／`M`（1サイクルで終わるが設計判断を伴う）／`L`（複数サイクルにまたがる、または着手前に方針を相談すべき大きな機能）。
- `/evolve` は **`S` と `M` だけを自動で拾う**。`L` は拾わない（人間がサイズを分解して `S`/`M` に落とすか、方針を決めてから着手する）。
- 優先度は上から順。追加するときは狙う位置に挿入する（末尾ではなく）。

## 未着手

- [ ] S サービスの速度(km/h)を表示する — サーブを打った瞬間の初速（`serve()` が `solveShot()` で解いた直後の `Math.hypot(ball.vx, ball.vy, ball.vz)`）を 3.6 倍して km/h でHUDに出す。人間・CPU/AIどちらのサーブでも出し、次のポイントが始まるまで（あるいは `config.js` に置いた表示秒数のあいだ）スコアボード脇に残す。DOM を触るのは `hud.js` だけ・表示秒数などの数値は `config.js` に集約、という既存の約束を守ること。受け入れ条件：フルパワーのフラットサーブとセカンド相当の緩いサーブで、表示される値が明確に違い、実測の初速と一致すること（テストは初速→km/h の換算関数を純ロジックとして検証する）。

- [ ] M ネットに出た相手への CPU の攻め手を多様化する — 現在 `ai.js` の `cpuShot()` は、相手がネットから `CPU.NET_Z`(4.0m) 以内にいると一律 `CPU.LOB_VS_NET`(0.5) の確率でロブを選ぶだけなので、ネットに出ると常にロブが飛んできて単調（ユーザー報告「安易にロブを多用している」）。少なくとも次の3択を状況で撃ち分けたい：①足元へ沈める短い球（ネットに近い相手ほど有効）②サイドライン際へ低く速いパッシング（相手が中央に寄っているほど有効）③ロブ（相手が前に詰め切っているときだけ＝意表をつく1本）。相手の x/z と、どれだけ詰めているかで重み付けする。受け入れ条件：人間がネット際（z≒-2）にいるときのロブ率が現状の50%から明確に下がり（目安2〜3割）、代わりにパッシングと沈める球が出ること。前に出過ぎている（z が `PLAYER.Z_NEAR` 寄り）ときだけロブの比率が上がること。シングルスのベースライン同士のラリーの配球（`LOB_BASE` 経由）は変えないこと。

- [ ] M 角度をつけたサービス（決まりやすいがフォールトもしやすい）を用意する — 現在のワイドは `SERVE.AIM_WIDE_MIN`(2.75)〜`AIM_WIDE_MAX`(3.85) の範囲で、コースを選んでも「速いか遅いか」以上の駆け引きが乏しい。より外へ切れる角度のコースを用意し、その代わりフォールト率が上がる（サービスボックスを外れやすい／`SERVE.CLEARANCE` が厳しくなる）ようにして、リスクとリターンを釣り合わせる。受け入れ条件：新しい角度のサーブは、成功すればレシーバーの届く範囲の外（＝エースになりやすい）へ切れること、フォールト率が従来のワイドより有意に高いこと、T・ボディの既存のバランスは変えないこと。人間と CPU/AI の両方が使えること。

## 進行中

（なし）
